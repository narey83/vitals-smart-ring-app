<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!--
        The JieLi firmware-update AAR declares these for its own file-picker and task handling;
        Vitals needs neither — it writes the image only to its own cache and never reorders tasks
        — so they are stripped rather than let the update feature widen the app's permissions. See
        FirmwareUpdate.kt and vitals/libs.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.REORDER_TASKS" tools:node="remove" />

    <!--
        For one request only: asking GitHub, once a day, whether a newer release is out. No
        reading is ever sent anywhere, and the check can be switched off in Settings — see
        Updates.kt. Until 0.2.0 there was no INTERNET permission at all. Where it is a permission
        the wearer grants ("Network", on GrapheneOS), it is asked for during setup.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
        "Sensors", on the builds that have it: asked for during setup alongside Network. Vitals
        reads nothing from the phone's own sensors. Standard Android has no such permission and
        ignores this line. See VitalsActivity.setUp.
    -->
    <uses-permission android:name="android.permission.OTHER_SENSORS" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- connectedDevice, not health: the work is holding a Bluetooth link open. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- And location, only while a workout the wearer started is recording its route. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Alarms and the collector do not survive a restart; this books and starts them again. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Asks, once, to be left off battery optimisation — see VitalsActivity.runInBackground. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!--
        Bluetooth scanning needed location up to Android 11. From 12 it does not (see
        neverForLocation above), and location is asked for only when a walk, run or ride is first
        started, to record its route from the phone's GPS. Never in the background: the route is
        followed only for a workout the wearer started, while its notification is showing. See
        Route.kt.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Health Connect is on-device: these let readings be shared with other apps on this phone,
         at the wearer's choice, and never over a network. -->
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.WRITE_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Vitals">
        <activity
            android:name=".VitalsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE"
            android:targetActivity=".VitalsActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <receiver android:name=".BedtimeReceiver" android:exported="false" />

        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".CollectorService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location" />
    </application>
</manifest>
